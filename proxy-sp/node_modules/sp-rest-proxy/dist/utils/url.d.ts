import { Request } from 'express';
import { IProxyContext, IProxySettings } from '../core/interfaces';
export declare class UrlUtils {
    private ctx;
    private settings;
    constructor(ctx: IProxyContext, settings: IProxySettings);
    isOnPrem: (url: string) => boolean;
    isHttps: (url: string) => boolean;
    isAbsolute: (url: string) => boolean;
    apiEndpoint: (req: Request | string) => string;
    proxyEndpoint: (reqUrl: string) => string;
}
